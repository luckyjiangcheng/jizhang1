# 验证清单

## 家庭共享
- [x] 家庭成员最多 5 人（第 6 人邀请被拒绝且提示明确）
- [x] 家庭成员列表可正常获取，权限校验正确

## 云端存储与多设备同步
- [x] 交易新增/更新/查询在云端持久化，刷新后数据一致
- [x] 交易软删除生效，默认列表不返回已删除记录
- [x] 增量同步参数可用（updated_since/include_deleted），跨设备删除可同步

## 高级统计
- [x] 统计口径仅支出（排除收入/负数），个人与家庭口径一致
- [x] 支出金额支持当日/本周/本月/年度汇总，并输出较昨天/较上周/较上月/较去年波动
- [x] 支出笔数具备同等周期与波动输出
- [x] 预测接口输出稳定（无数据/少量数据场景可解释）

## AI 增强
- [x] 文本识别在 AI 返回 code fence/非严格 JSON 时仍可解析
- [x] 图片识别可用且分类落在产品分类集合
- [x] AI 失败时返回信息可定位（不泄露敏感信息）

## 数据导出
- [x] CSV 导出按筛选条件返回正确数据
- [x] Excel 导出生成可打开的 .xlsx，字段齐全且编码正确
- [x] PDF 导出生成可打开的 PDF，包含汇总与明细

## 预算与提醒
- [x] 支持个人预算与家庭预算（可见性与权限符合 Spec）
- [x] 预算状态“已花费/剩余/是否超支”计算正确（家庭预算统计全家）
- [x] 超预算提醒可被客户端轮询获取/展示

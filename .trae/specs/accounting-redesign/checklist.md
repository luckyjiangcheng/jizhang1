# Checklist

- [x] Unified Design Variables are correctly defined in both `confirm.txt` and `dashboard.txt`.
- [x] Both pages use the standardized primary color (`#007aff`).
- [x] Both pages use the same border-radius, shadow, and spacing scale.
- [x] Confirmation Page (`confirm.txt`) has a clean, compact, and consistent card style.
- [x] Category Chips in `confirm.txt` have a distinct and polished "active" state.
- [x] Dashboard Page (`dashboard.txt`) metric cards match the Confirmation card style.
- [x] ECharts in `dashboard.txt` are minimalist, with a coordinated color palette and rounded segments.
- [x] Transaction List in `dashboard.txt` is clean, readable, and consistent.
- [x] Dark Mode is robust and consistently applied across all UI elements and charts in both files.
- [x] Both pages are fully responsive and optimized for mobile devices.
- [x] The `public` folder is updated via `python3 scripts/build.py`.
- [x] Visual smoke test confirms a "concise and unified" style across both pages.
